name: Plugin version

on:
  pull_request_target:
    branches: [main]
    types: [opened, synchronize, reopened, edited, ready_for_review]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: plugin-version-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  version:
    if: >-
      !github.event.pull_request.draft &&
      github.event.pull_request.head.repo.full_name == github.repository
    uses: Mikode13/.github/.github/workflows/plugin-version.yml@b682c98075a68c9041f1efbf0dbe5a927e688d3f
    with:
      client_id: ${{ vars.MIKODE_SKILLS_VERSION_APP_CLIENT_ID }}
    secrets:
      private_key: ${{ secrets.MIKODE_SKILLS_VERSION_APP_PRIVATE_KEY }}
